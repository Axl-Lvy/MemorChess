ktor:
    application:
        modules:
            - proj.memorchess.axl.server.ApplicationKt.module
    deployment:
        port: 8080

database:
    url: jdbc:postgresql://localhost:5432/memorchess
    user: postgres
    password: postgres
