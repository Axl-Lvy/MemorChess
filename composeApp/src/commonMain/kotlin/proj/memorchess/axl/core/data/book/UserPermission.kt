package proj.memorchess.axl.core.data.book

/** User permissions that can be assigned to users. */
enum class UserPermission(val value: String) {
  /** Permission to create and manage books. */
  BOOK_CREATION("BOOK_CREATION")
}
