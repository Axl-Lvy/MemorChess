package proj.memorchess.axl.ui.components.settings

/**
 * Button parameters interface.
 *
 * The only way to use it is through pattern matching over its implementations.
 */
interface ButtonParameters
